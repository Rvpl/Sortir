{{ form_start(form) }}
    {{ form_widget(form) }}


    <button class="btn">{{ button_label|default('Enregistrer') }}</button>
    <button class="btn">{{ button_label|default('Publier ton événement') }}</button>
    <button class="btn">{{ button_label|default('Annuler') }}</button>
{{ form_end(form) }}
